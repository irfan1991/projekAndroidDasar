<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:padding="20dp"
    android:background="#fff"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:context="com.bagicode.www.navigationscreen.ScrollingActivityRegister"
    tools:showIn="@layout/activity_scrolling_register">

    <EditText
        android:textColorHint="#000"
        android:drawablePadding="10dp"
        android:drawableLeft="@drawable/ic_username"
        android:id="@+id/username"
        android:hint="Username"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />

    <EditText
        android:textColorHint="#000"
        android:drawablePadding="10dp"
        android:drawableLeft="@drawable/ic_password"
        android:layout_marginTop="10dp"
        android:id="@+id/password"
        android:layout_below="@id/username"
        android:hint="Password"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />

    <EditText
        android:layout_marginBottom="20dp"
        android:textColorHint="#000"
        android:drawablePadding="10dp"
        android:drawableLeft="@drawable/ic_mail"
        android:layout_marginTop="10dp"
        android:id="@+id/mail"
        android:layout_below="@id/password"
        android:hint="Mail Adress"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />

    <Button
        android:textColor="#000"
        android:layout_marginRight="10dp"
        android:layout_marginTop="10dp"
        android:textStyle="italic"
        android:background="#00000000"
        android:text="Login with my account"
        android:layout_below="@id/mail"
        android:id="@+id/btn_forget"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content" />

    <Button
        android:onClick="goAktivityUtamaReg"
        android:id="@+id/tbn_sigin"
        android:background="@color/colorAccent"
        android:text="SIGN UP"
        android:layout_below="@id/mail"
        android:layout_toRightOf="@id/btn_forget"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content" />

</RelativeLayout>
